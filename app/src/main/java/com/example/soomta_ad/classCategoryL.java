package com.example.soomta_ad;

import android.os.Bundle;

import androidx.annotation.Nullable;
import androidx.appcompat.app.AppCompatActivity;

public class classCategoryL extends AppCompatActivity {
    @Override
    protected void onCreate(@Nullable Bundle saveInstanceStare){
        super.onCreate(saveInstanceStare);
        setContentView(R.layout.class_category_l);
    }
}
